# Node 의존성 — server/ 와 web/ 두 곳에 있다
node_modules/
server/node_modules/
web/node_modules/

# 프런트 빌드 산출물. setup-dashboard.sh --build 가 만든다
web/dist/

# 편집기·OS
.idea/
*.iml
.DS_Store
Thumbs.db
*.swp

# 로그
*.log
logs/

# 이 서비스는 비밀을 담지 않는다.
# DB 비밀번호는 database/secrets/ 에 있고 install.sh 가 읽어 /etc/kamailio/ 로만 넣는다.
# 혹시라도 여기 생기면 커밋되지 않게 막아 둔다.
secrets/
*.pw
