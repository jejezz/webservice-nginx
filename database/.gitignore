# DB 사용자 비밀번호 — 절대 커밋 금지
secrets/

# 백업(덤프)과 이전 설정 파일
backups/

# 장비마다 다른 [server] 값. 구축 마법사의 폼과 사람이 settings.ini 를 쓰고,
# setup_mariadb.sh 가 읽는다. .applied-settings 는 실제로 반영한 값의 기록이라
# root 가 쓴다. 항목 정의(settings-schema.json)는 커밋한다.
settings.ini
.applied-settings
