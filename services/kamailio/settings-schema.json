{
  "service": "kamailio",
  "applyCommand": "sudo ./install.sh --apply",
  "settingsFile": "settings.ini",
  "appliedFile": ".applied-settings",
  "fields": [
    {
      "key": "sip_domain",
      "label": "SIP 도메인",
      "help": "계정이 등록될 도메인입니다. **비워 두면 사이트 값(site/settings.ini)을 씁니다** — 이 장비만 다르게 두어야 할 때만 채우세요.",
      "placeholder": "pluto.org",
      "optional": true,
      "type": "text",
      "pattern": "^[A-Za-z0-9]([A-Za-z0-9.-]*[A-Za-z0-9])?$",
      "patternHint": "도메인 (예: pluto.org)",
      "effect": "kamctlrc 의 SIP_DOMAIN 과 kamailio-local.cfg 의 alias 로 함께 들어갑니다. websocket-relay 도 같은 값을 써야 하므로 사이트 값에 두는 편이 안전합니다 — 어긋나면 계정은 만들어지는데 등록이 안 됩니다."
    },
    {
      "key": "sip_listen_addr",
      "label": "SIP 를 받을 주소",
      "help": "이 장비의 LAN 주소입니다. 장비마다 다르므로 기본값이 없습니다.",
      "placeholder": "192.168.0.252",
      "optional": false,
      "type": "ipv4",
      "pattern": "^\\d{1,3}(\\.\\d{1,3}){3}$",
      "patternHint": "IPv4 (예: 192.168.0.252)",
      "effect": "kamailio-local.cfg 의 listen=udp/tcp:<주소>:5060 이 됩니다. listen 을 하나라도 적으면 Kamailio 는 자동 바인딩을 멈추므로, 이 주소와 127.0.0.1 만 열립니다. 이 장비에 없는 주소를 적으면 기동에서 죽습니다."
    },
    {
      "key": "sip_push_url",
      "label": "착신 푸시를 요청할 곳",
      "help": "자고 있는 모바일을 깨울 때 Kamailio 가 부르는 주소입니다. 같은 장비의 websocket-relay 입니다.",
      "placeholder": "http://127.0.0.1:28099/sip-push",
      "optional": false,
      "default": "http://127.0.0.1:28099/sip-push",
      "type": "text",
      "pattern": "^https?://[^\\s]+$",
      "patternHint": "http(s) 주소",
      "effect": "kamailio-local.cfg 의 SIP_PUSH_URL 이 됩니다 (docs/incoming-call.md ⑤)."
    }
  ]
}
