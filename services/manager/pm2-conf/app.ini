# 관리 대시보드 프로세스 선언
# 스키마: ../../../docs/pm2-conf.md

[app]
name   = manager
cwd    = server
script = src/index.js

[env]
NODE_ENV = production
# nginx-conf/service.ini 의 ports, config.json 의 port 와 같아야 한다.
PORT     = 28084
